import React from 'react';
import {Navbar} from 'reactstrap';

const CustomHeader = () => {
    return(
        <header>
            <Navbar>
                
            </Navbar>
        </header>
    );
}

export default CustomHeader;